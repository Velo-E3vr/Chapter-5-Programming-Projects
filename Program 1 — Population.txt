Start
    Prompt user for starting population, percent increase, and days
    While start < 2 OR percent < 0 OR days < 1:
        Display "Invalid input"
        Re-prompt for all three
    Set population = start
    Set rate = percent / 100
    For day = 1 to days:
        population = population + population * rate
        Display day and population
End